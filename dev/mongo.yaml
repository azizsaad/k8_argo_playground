apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: nanajanashia/argocd-app:1.2
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080




# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: mongodb-deployment
# spec:
#   replicas: 2
#   selector:
#     matchLabels:
#       app: mongodb
#   template:
#     metadata:
#       labels:
#         app: mongodb
#     spec:
#       containers:
#       - name: mongodb
#         image: mongo
#         ports:
#         - containerPort: 27017
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: mongodb-service 
# spec:
#   selector:
#     app: mongodb
#   ports:
#   - port: 27017
#   protocol: TCP
#   targetPort: 27017